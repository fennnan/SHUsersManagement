@model IEnumerable<MvcUI.Models.UserDto>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.UserName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Roles)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.UserName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.RolesList)
<!-- atforeach (var rolItem in item.Roles) {
                atrolItem
} -->
            </td>
            <td>
                <a asp-action="Edit" asp-route-userName="@item.UserName">Edit</a> |
                <a asp-action="Details" asp-route-userName="@item.UserName">Details</a> |
                <a asp-action="Delete" asp-route-userName="@item.UserName">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
